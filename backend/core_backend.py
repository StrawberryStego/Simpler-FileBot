from typing import List

from backend.media_record import MediaRecord


def match_records(database, media_records: List["MediaRecord"]) -> List["MediaRecord"]:
    print(database, media_records)
    return []
